.home
  display: flex
  justify-content: center
  width: 100%
  background-color: bgColor--primary
  &-container
    display: grid
    grid-template: auto
    grid-row-gap: 50px
    grid-column-gap: 28px