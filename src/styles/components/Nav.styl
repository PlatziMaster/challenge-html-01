.nav
  display: flex
  flex-direction: column

.nav__item
  width: 100%
  padding: 0.8em
  font-family: fontFamily--primary
  font-size: fontSize--small
  font-weight: 500
  text-transform: uppercase
  text-align: center
  line-height: 21px
  letter-spacing: 1.75px
  &:hover
    background-color: rgba(colorBrand--secondary, 36%)