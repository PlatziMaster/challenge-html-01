import React from 'react';
import Logo from './Logo';
import Nav from './Nav';
import MenuIcon from './MenuIcon';

const Header = () => {
  return (
    <header>
      <Logo />
      <Nav />
      <MenuIcon />
    </header>
  );
};

export default Header;
